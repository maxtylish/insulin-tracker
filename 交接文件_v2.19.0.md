# 🩺 胰島素記錄表 PWA — 交接文件（v2.19.0）

## 專案位置

| 項目 | 路徑/URL |
|------|---------|
| 本機 | `D:\myapps\insulinrecorder\` |
| GitHub | `maxtylish/insulin-tracker` |
| 線上 | https://maxtylish.github.io/insulin-tracker/ |

---

## 技術重點

- **單一 HTML 檔**（`index.html`），約 370KB+，無 build 工具
- Supabase 雲端同步 + localStorage 雙儲存
- Nightscout API：`https://0977771916.t1ns.tw`
- Chart.js 4.4.1、Service Worker（cache **`insulin-tracker-v6`**）
- `SITE_ALARM_MAP`：肚子=15min / 手臂=20min / 左右大腿=30min

---

## ⚠️ 重要操作規則

1. **禁止**用 Edit tool 直接改大檔案（會截斷）
2. 所有修改必須用 **Python str.replace() patch 腳本**，完成後 `node --check` 驗證 JS 語法
3. Python `"""..."""` 字串內 `\'` 會被解釋為 `'`，JS 內單引號改用 `\\'`
4. Git 指令必須在本機執行（Sandbox 無法操作 Windows NTFS 上的 .git）
5. **每次修改（不論大小）都必須更新 `APP_VERSION` + changelog `<ul>`**（語意版本：功能 → minor，修正 → patch）
6. GitHub Pages 從 `main` 自動部署，`git push` 即可，**不需要** `npx gh-pages`

---

## 當前版本狀態（v2.19.0）

```javascript
const APP_VERSION = 'v2.19.0';  // 位置：index.html ~8483行，必須在 init() 之前
const CACHE = 'insulin-tracker-v6';  // sw.js 第1行
```

---

## 關鍵程式碼位置

### 模組變數（~7971行）
```javascript
var _pipLastRapid = null;     // last rapid injection {dt, site} for PiP display
var _pipLastBasal = null;     // last basal injection {dt, site} for PiP display
var _pipCarouselIdx = 0;      // PiP event carousel current index
let _insCountdownTimer = null; // 1-sec ticker for rapid insulin countdown
let cgmTimeTicker = null;      // 1-min ticker to refresh "X 分鐘前"
```

### `_compactInsRow` 三段式邏輯
```javascript
// 過期
timeStr = durationH <= 4 ? '作用結束' : '峰值結束';
// 起效前（速效顯示秒）
timeStr = _om + 'm' + String(_os).padStart(2,'0') + 's 後起效';
// 起效中（速效顯示秒）
timeStr = _rh > 0
  ? (_rh + 'h' + _rm + 'm' + String(_rs).padStart(2,'0') + 's')
  : (_rm + 'm' + String(_rs).padStart(2,'0') + 's');
```

### CGM Panel 胰島素區塊（~10117行）
```javascript
var pr = lastRapid ? _compactInsRow(lastRapid, 3,  '#fb923c', '⚡', '速效') : '';
var pb = lastBasal ? _compactInsRow(lastBasal, 12, '#c084fc', '💉', '長效峰值') : '';
var prFinal = pr || '<div class="cgm-insulin-row" ...>⚡ 速效 · 無作用中</div>';
```

### `calcIOBRemainingMins()` future filter（~8076行）
```javascript
function checkInj(dateStr, timeStr, siteKey) {
  if (!timeStr) return;
  var injMs = new Date(dateStr + 'T' + timeStr + ':00').getTime();
  if (injMs > now) return;  // 過濾未來施打時間，避免電池永遠100%
  var onsetMin = (SITE_ALARM_MAP[siteKey] && SITE_ALARM_MAP[siteKey].def) || 20;
  var endMs = injMs + onsetMin * 60000 + DURATION_H * 3600000;
  var remain = endMs - now;
  if (remain > maxRemainMs) maxRemainMs = remain;
}
```

### `startCGMPolling()` 計時器架構
```javascript
if (_insCountdownTimer) clearInterval(_insCountdownTimer);
_insCountdownTimer = setInterval(updateBasalTimeBar, 1000); // 每秒更新倒數
cgmPollTimer = setInterval(pollCGM, 5 * 60000);            // CGM 每5分鐘
cgmTimeTicker = setInterval(_tickCGMTime, 60000);          // 時間標籤每分鐘
updateBasalTimeBar();                                       // 立即執行
```

### PiP IOB 電池（`updateCGMPip` ~8336行）
```javascript
// 電池百分比：calcIOBRemainingMins()（速效 3h 計算，已過濾未來注射）
// iobSubEl 格式：1h47m / 47m（無作用中時顯示空字串）
if (iobSubEl) {
  if (remMins > 0) {
    var _irh = Math.floor(remMins / 60), _irm = remMins % 60;
    iobSubEl.textContent = _irh > 0 ? _irh + 'h' + _irm + 'm' : _irm + 'm';
  } else {
    iobSubEl.textContent = '';
  }
}
```

---

## 已完成功能清單（截至 v2.19.0）

| 版本 | 日期 | 功能 |
|------|------|------|
| v2.19.0 | 2026-05-21 | PiP 電池過濾未來注射時間（避免永遠100%）+ 還原速效剩餘時間文字 |
| v2.18.0 | 2026-05-21 | 血糖單位 mg/dL CSS 修正（position:absolute 不溢出）+ PiP/CGM 三項顯示調整 |
| v2.17.0 | 2026-05-21 | 速效倒數精確到秒 + 1秒計時器 + CGM 無作用中 placeholder + PiP 長效同步 + SW v6 |
| v2.16.0 | 2026-05-21 | 4 skin 主題系統、胰島素停用功能、saveDayData 修正、mealParams 持久化 |
| v2.15.0 | 2026-05-19 | 套餐下拉、進位提示、點心子項目、手動雲端同步 |
| v2.14.0 | 2026-05-18 | 行事曆整合、補糖自動儲存、速效進度條 |
| v2.13.0 | 2026-05-17 | Google Calendar PKCE OAuth、月曆頁 |
| v2.12.0 | 2026-05-16 | 胰島素多瓶管理（NovoRapid/Tresiba）、7天到期警報 |
| v2.11.0 | 2026-05-16 | 統計週一起始、施打部位預設 |
| v2.9.x  | 2026-05-16 | 補打/補糖完整 UI、施打部位連動起效時間、跨夜偵測 |
| v2.8.x  | 2026-05-15 | 待辦 Supabase 同步、可點擊通知跳轉、重要待辦星號 |

---

## 已知待處理問題

1. **Supabase 待辦刪除不跨裝置同步**：無軟刪除機制，裝置 B 重開會「復活」已刪項目。修法：加 `deletedAt` 欄位或 `updated_at` 時間戳決定優先順序。

---

## Git 工作流程

```bash
# 本機執行（Sandbox 無法操作 .git）
git add index.html sw.js
git commit -m "v2.XX.X: 功能描述"
git push
# GitHub Pages 自動從 main 部署，無需額外指令
```
