# 胰島素紀錄表 — 上線步驟說明

> 完成後您將得到一個可在手機和電腦同步使用的網址，不需要登入。
> 全程免費，約 10 分鐘完成。

---

## 第一步：建立 Supabase 資料庫（免費）

1. 前往 [https://supabase.com](https://supabase.com) → 右上角 **Start your project** → 免費註冊

2. 建立新專案：
   - **Name**：`insulin-tracker`
   - **Database Password**：設一個密碼（自己記住）
   - **Region**：選 `Northeast Asia (Tokyo)`
   - 點 **Create new project**，等約 1 分鐘

3. 左側選單點 **SQL Editor** → 貼入以下 SQL → 點 **Run**：

```sql
CREATE TABLE insulin_records (
  user_id    TEXT NOT NULL,
  date       TEXT NOT NULL,
  data       JSONB NOT NULL DEFAULT '{}',
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  PRIMARY KEY (user_id, date)
);

ALTER TABLE insulin_records ENABLE ROW LEVEL SECURITY;
CREATE POLICY "allow_all" ON insulin_records
  FOR ALL USING (true) WITH CHECK (true);
```

4. 左側點 **Project Settings** → **API**，複製：
   - **Project URL**（例如 `https://abcdefgh.supabase.co`）
   - **anon / public** 金鑰

---

## 第二步：部署到 GitHub Pages（免費）

### 2-1 建立 GitHub 帳號
前往 [https://github.com](https://github.com) 免費註冊（已有帳號跳過）

### 2-2 建立新 Repository
1. 登入後點右上角 **＋** → **New repository**
2. **Repository name**：填 `insulin-tracker`
3. 選 **Public**（GitHub Pages 免費版需要 Public）
4. 勾選 **Add a README file**
5. 點 **Create repository**

### 2-3 上傳 index.html
1. 在新建的 repo 頁面，點 **Add file** → **Upload files**
2. 把 `index.html` 拖曳上去
3. 下方 Commit message 填 `initial upload`
4. 點 **Commit changes**

### 2-4 開啟 GitHub Pages
1. 點上方 **Settings** → 左側選單 **Pages**
2. **Source** 選 **Deploy from a branch**
3. **Branch** 選 `main`，資料夾選 `/ (root)`
4. 點 **Save**
5. 等約 1 分鐘，頁面上會出現您的網址：
   `https://您的帳號名.github.io/insulin-tracker/`

---

## 第三步：填入 Supabase 設定

1. 用瀏覽器打開您的 GitHub Pages 網址
2. 切換到 **⚙️ 設定** 頁面
3. 找到「☁️ 雲端同步設定」區塊：
   - **Supabase URL**：貼上第一步的 Project URL
   - **Anon Key**：貼上第一步的 anon/public 金鑰
4. 點 **儲存並連線** → 看到「✅ 已連線至 Supabase」即成功

---

## 第四步：手機跨裝置同步

1. 電腦設定好後，進入 **⚙️ 設定** → 「帳號 ID」→ 點 **複製 ID**
2. 手機用瀏覽器打開同樣的 GitHub Pages 網址
3. 進入 **⚙️ 設定** → 「在另一台裝置上使用相同帳號？」→ 貼入 ID → **套用**
4. 手機自動同步電腦資料 ✅

---

## 日後更新網頁

如果您修改了 `index.html` 想要更新網頁：
1. 進入 GitHub repo
2. 點 `index.html` → 點右上角鉛筆圖示（Edit）→ 或直接重新上傳
3. Commit changes → 約 1 分鐘後網頁自動更新

---

## 常見問題

**Q：資料安全嗎？**  
資料存在您自己的 Supabase 帳號，只有知道網址的人才能使用。

**Q：免費方案限制？**  
Supabase 免費 500MB 資料庫、GitHub Pages 免費無限靜態流量，都夠用。

**Q：忘記帳號 ID 怎麼辦？**  
Supabase 後台 → Table Editor → `insulin_records` 表格中可以查到您的 `user_id`。

