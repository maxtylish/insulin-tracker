# 改良紀錄 v2.52.0（2026-06-11）— 介面美化＋錯誤修正

## 還原方式（如發現 bug）

**整版還原（最快）：**
```powershell
Copy-Item backups\index_v2.51.1_pre-beautify.html index.html -Force
Copy-Item backups\sw_v2.51.1_pre-beautify.js sw.js -Force
```
備份檔 md5 與修改前 index.html 完全一致（`9c5647bf62640fe8eb1c2521ab3a3983`）。

**只還原 UI 美化（保留錯誤修正）：**
刪除 index.html 內 `v2.52.0 UI POLISH LAYER` 起始註解到 `END v2.52.0 UI POLISH LAYER` 的整個 CSS 區塊（位於 `</style>` 前，約 120 行，獨立區塊不影響其他樣式）。

**Git 還原：**
```powershell
git log --oneline -5
git revert <本次commit的hash>
```

---

## 完整檢查結果（檢查於 v2.51.1）

### ✅ 通過項目
| 項目 | 結果 |
|------|------|
| JS 語法（node --check，9290 行） | ✅ 通過 |
| 版本一致性（APP_VERSION = changelog 最新版） | ✅ v2.51.1 |
| sw.js cache 版本 | ✅ v7 |
| JSON.parse 防護 | ✅ 全部在 try/catch 內 |
| 頂層變數重複宣告 | ✅ 無 |
| CSS 大括號平衡 | ✅ 0 差 |

### 🔴 高風險問題（本次已修正）
1. **calcOverlay 計算機 modal 重複兩份**
   - 位置：L15370（新版，存留）/ L15578–15724（舊版殘留 147 行，已移除）
   - 舊版含過時預設：ISF 50、目標血糖 110（現行為 ISF 110、目標 150/160/170）
   - 造成 28 個重複 id；瀏覽器 getElementById 只取第一份所以未發病，但屬地雷
2. **4 個 CSS 變數從未定義**：`--surface`、`--hover`、`--primary-rgb`、`--text-primary`
   - 實際症狀：事件篩選鈕／使用者切換鈕 active 無背景色、待辦月曆 hover 無效、部分輸入框背景透明
   - 已在 :root 與各 skin 補定義
3. **自訂主題缺 --primary-rgb**：`_applyCustomSkinStyle()` 已加自動推導（由 --primary hex 計算 RGB 三元組）

### 🟡 低風險問題（僅記錄，未修改）
4. 死代碼：`toggleHideDone` / `onVialSearch` / `clearVialSearch` 對應 DOM 元素已不存在（`hide-done-btn`、`vial-search-input/clear`、`todo-sub-list/cal`），全有 null 防護不會 crash，可日後清理
5. `'noverapid'` 拼字錯誤（應為 novorapid）共 6 處（L3136/3152/3168/5592/5596/5600 附近）— 程式只判斷 `=== 'tresiba'`，功能完全正常
6. `tick` / `checkInj` 函式名重複 — 各在不同函式作用域內，無衝突，不需處理

---

## 修改步驟紀錄

| # | Patch 腳本 | 內容 | 驗證 |
|---|-----------|------|------|
| 0 | — | 備份 index.html + sw.js 至 backups/（md5 驗證一致） | ✅ |
| 1 | patch1_highrisk.py | 移除舊版 calcOverlay 區塊（147 行）；補 4 個 CSS 變數定義（:root + 4 skins）；自訂主題推導 --primary-rgb | node --check ✅ |
| 2 | patch2_polish.py | 在 `</style>` 前附加「v2.52.0 UI POLISH LAYER」獨立 CSS 區塊 | CSS 括號平衡 ✅ |
| 3 | patch3_version.py | APP_VERSION → v2.52.0、新增 changelog、sw.js cache v7 → v8 | node --check ✅ |

## UI 美化內容（Polish 層，12 項）

1. 質感 tokens：`--radius-sm/lg`、`--shadow-card(-hover)`、`--focus-ring`、`--ease-out`
2. 頁面切換淡入 0.22s（只用 opacity，不用 transform，避免影響 fixed 定位的 FAB/PIP）
3. 卡片柔和雙層陰影統一（period/overview/stat/history-day/date-bar/legend）
4. 輸入框焦點：主題色光暈 `rgba(var(--primary-rgb), .16)`
5. focus-visible 鍵盤導覽外框（無障礙）
6. 導覽列 active icon 微上移 + 點按底色回饋
7. 按鈕觸覺回饋：save-btn / date-nav-btn / 計算機 FAB 按壓縮放
8. 計算機 modal 進場上滑動畫 0.24s
9. 統計數值 `tabular-nums` 等寬數字防跳動
10. 桌面捲軸主題色樣式（`@media (hover:hover)` 限定，不影響手機）
11. 標題 letter-spacing 微調
12. 黃土（warm-dark）卡片加細邊框增加深底層次

所有動畫均支援 `prefers-reduced-motion: reduce` 自動停用。

## 最終驗證（v2.52.0）

- node --check index.html 內嵌 JS ✅
- node --check sw.js ✅
- CSS 大括號平衡 ✅
- calcOverlay 僅存 1 份 ✅
- 重複靜態 id：0 ✅
- APP_VERSION = changelog = v2.52.0 ✅、sw cache v8 ✅
- 檔案行數：15835 → 15829（−147 死代碼 +141 新增）

## Git Commit

```powershell
git add .
git commit -m "UI polish layer + remove dead calcOverlay + fix missing CSS vars (v2.52.0)"
git push
```
